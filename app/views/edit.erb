<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit <%= @recipe.name %></title>
  </head>
  <body>
    <h1>Now Editing: <%= @recipe.name %>
      <form action="recipes/<%= @recipe.id %>" method="post">

      </form>
  </body>
</html>
